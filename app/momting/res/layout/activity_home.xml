<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <FrameLayout
        android:id="@+id/realtabcontent"
        android:layout_width="fill_parent"
        android:layout_height="0dip"
        android:layout_weight="1" />

    <android.support.v4.app.FragmentTabHost
        android:id="@android:id/tabhost"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:visibility="gone" >
    </android.support.v4.app.FragmentTabHost>

    <LinearLayout
        android:id="@+id/main_tab_group"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:gravity="bottom"
        android:orientation="horizontal" >

        <RadioButton
            android:id="@+id/main_tab_home"
            style="@style/TabButton"
            android:checked="true"
            android:drawableTop="@drawable/tab_icon_home_selector"
            android:text="首页" />

        <RadioButton
            android:id="@+id/main_tab_find"
            style="@style/TabButton"
            android:drawableTop="@drawable/tab_icon_find_selector"
            android:text="哈哈" />

        <RadioButton
            android:id="@+id/main_tab_friend"
            style="@style/TabButton"
            android:drawableTop="@drawable/tab_icon_friend_selector"
            android:text="哈哈" />

        <RadioButton
            android:id="@+id/main_tab_setting"
            style="@style/TabButton"
            android:drawableTop="@drawable/tab_icon_setting_selector"
            android:text="哈哈" />
    </LinearLayout>

</LinearLayout>